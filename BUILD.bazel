load("@rules_rust//crate_universe:defs.bzl", "crate", "crates_vendor")

crates_vendor(
    name = "crate_vendor",
    mode = "remote",
    packages = {
        "libffi": crate.spec(version = "3.2.0"),
    },
    tags = ["manual"],
    vendor_path = "crates",
)
